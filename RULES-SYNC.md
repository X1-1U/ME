# 外部规则集同步

每 6 小时检查一次上游（UTC 00:17、06:17、12:17、18:17）；也可以在 Actions → Sync upstream rules → Run workflow 手动触发。GitHub 定时任务可能延迟，公开仓库长期无活动可能被 GitHub 暂停定时运行。

- 只同步 rules-sources.json 中的 62 个外部公开规则源，输出到 rules/managed/。
- 根目录原有自定义文件完全不动、不下载、不复制、不去重。配置里的 Myother、Mylove、Myrule、Mydirect 独立引用原路径。
- ACL4SSR 追加的 9 个规则集完整保留上游内容。其他外部规则只删除同集合重复、相同策略组与相同解析标志下的等价重复，不裁剪不同策略的重叠。
- MRS 来源使用同仓库同目录 .list 文本源，配置对应 format: text。
- 每次先全部下载并验证，再生成结果。任何源失败则任务失败、不发布新提交，保留上一版。
- status.json 记录来源、哈希、条目数量与删除数量。没有内容变化就不提交。
- 上游跟踪的是 manifest 指定路径的最新内容；上游改名/删除会使任务失败，需要修正 manifest，不会静默删空规则。
- 若分流顺序或策略组发生变化，应同步更新 manifest 的 rules，避免按旧的策略去重。

规则来源归原仓库维护者所有，源链接在 rules-sources.json。仅处理规则，不保存机场订阅、节点密码、控制面板密钥或完整私人配置。
